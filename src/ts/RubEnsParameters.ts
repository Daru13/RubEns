/**
 * Set of parameters at the application level.
 */
export class RubEnsParameters {
    /**
     * Automatically create a new document with default parameters on startup.
     * @type {boolean}
     */
    createDocumentOnStartup: boolean = true;
}
